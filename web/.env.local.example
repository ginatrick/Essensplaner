# Kopieren nach .env.local und befüllen. .env.local NIE committen (siehe .gitignore).

NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Server-only (Server Actions/Route Handler), nie NEXT_PUBLIC_ voranstellen.
# Bewusste Abweichung von der ursprünglichen Architektur (Edge Function) für
# Phase 1 — siehe Commit-Historie "Haiku-Fallback". In Vercel als "Sensitive"
# markieren.
ANTHROPIC_API_KEY=
